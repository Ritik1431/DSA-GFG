#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str;
    cout<<"Enter your Name:"<<endl;
    // cin>>str;   it will take only one word (ie. first name only)
    getline(cin,str);
    cout<<str;   
    return 0;
}